
export default function Home() {
  return (
    <main style={{ padding: 40, minHeight: '100vh', background: '#000', color: '#fff' }}>
      <h1>It works 🎉</h1>
      <p>This is served from <code>app/page.jsx</code>.</p>
      <p>Try: /login, /collections, /items, /profile (we’ll add these after home shows).</p>
    </main>
  );
}
